rules:
  - id: metavariable-regex-capture-group
    patterns:
      - pattern: hashlib.new("$REGEX")
      - metavariable-regex:
          metavariable: $REGEX
          regex: (?<ALG>Sha\d\d\d_\d\d\d)
    message: a hash $ALG was detected
    languages: [python]
    severity: INFO
