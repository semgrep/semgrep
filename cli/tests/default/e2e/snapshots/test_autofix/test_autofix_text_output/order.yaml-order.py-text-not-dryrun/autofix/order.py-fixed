# Several findings will overlap in the following array. The autofixes
# must be applied in the order in which the findings are discovered or
# in which they are reported (which is it?).

replaced_lines_a_to_b_OK
c
replaced_lines_d_to_f_OK
replaced_lines_g_to_h_OK
