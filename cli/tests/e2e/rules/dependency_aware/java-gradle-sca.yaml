rules:
  - id: java-gradle-sca
    pattern: bad()
    r2c-internal-project-depends-on:
        namespace: maven
        package: http
        version: <= 1.6.0
    message: oh no
    languages: [java]
    severity: WARNING
