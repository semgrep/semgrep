This folder contains a `.semgrepignore` file that semgrep-docs links to.
The new location is `/OSS/src/targeting/default.semgrepignore`.
Once semgrep-docs uses the new URL, we can delete this folder.
