taint-mode: Semgrep will now treat lambdas' parameters as fresh, so a taint rule
that to find double-delete's should no trigger on the code below:

```cpp
for (ListNode *node : list) {
	list.erase(node, [](ListNode *p) {
		delete p;
	});
}
```
