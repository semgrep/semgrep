rules:
  - id: test-ac-matching
    languages:
      - php
    message: Working!
    options:
      commutative_boolop: true
    pattern: |
      !check1($stuff) && !check2($stuff)
    severity: WARNING
