rules:
  - id: tainting
    languages:
      - python
    message: |
      This confirms taint mode works.
    pattern-sinks:
      - sink1(...)
    pattern-sources:
      - source1(...)
    pattern-sanitizers:
      - sanitize1(...)
    severity: ERROR

