rules:
  - id: tainting
    languages:
      - python
    message: |
      This confirms taint mode works.
    pattern-sinks:
      - sink(...)
      - sink1(...)
      - eval(...)
    pattern-sources:
      - source1(...)
    pattern-sanitizers:
      - sanitize(...)
    severity: ERROR

