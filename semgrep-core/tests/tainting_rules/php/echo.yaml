rules:
  - id: test-taint-echo
    mode: taint
    languages:
      - php
    message: Working!
    pattern-sources:
      - $_GET[...]
    pattern-sanitizers:
      - htmlspecialchars(...)
    pattern-sinks:
      - echo ...;
    severity: ERROR
