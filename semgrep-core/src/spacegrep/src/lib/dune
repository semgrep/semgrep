(library
  (public_name spacegrep)
  (name spacegrep)
  (libraries
     ANSITerminal
     unix

     semgrep_core_response
  )
  (preprocess
   (pps
     ppx_deriving.show
     ppx_deriving.eq
   ))
)

(ocamllex Lexer)
