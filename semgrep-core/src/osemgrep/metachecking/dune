;LATER: to merge with semgrep_metachecking at some point
(library
  (name osemgrep_metachecking)
  (wrapped false)
  (libraries

  )
 (preprocess
   (pps
     ppx_profiling
     ppx_deriving.show
     ppx_deriving.eq
   )
 )
)
