; Eventually, this will be merged with the /targeting library from semgrep-core

(library
  (name osemgrep_targeting)
  (wrapped false)
  (libraries
    semgrep_targeting
  )
 (preprocess
   (pps
     ppx_profiling
     ppx_deriving.show
     ppx_deriving.eq
     ppx_hash
   )
 )
)
