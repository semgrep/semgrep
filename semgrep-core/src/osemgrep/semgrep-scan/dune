; OCaml implementation of the 'semgrep scan' subcommand.
;
(library
  (name Semgrep_scan)
  (wrapped false)
  (libraries
    cmdliner
    commons  ; from pfff
    core_cli  ; semgrep-core's CLI
    osemgrep_util
  )
)
