class extends Component {
  users = [
    { id: 1, name: '1' },
    { id: 2, name: '2' },
    { id: 3, name: '3' },
    { id: 4, name: '4' },
  ];

  state = {};

  static propTypes = {
    onReady: PropTypes.func.isRequired,
  };

  componentDidMount() {
    this.props.onReady();
  }
};
