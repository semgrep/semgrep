<?php

function foo($a) {
  // some code remove the space after the cast, some don't ...
  return (bool) $a;
}

function foo($a) {
  yield result(new BackdatedTime(
    $assoc['time'] - TimelineConstants::ASSOC_BACKDATE_TIME_OFFSET,
    (int)$aassoc['data']
  ));
}
