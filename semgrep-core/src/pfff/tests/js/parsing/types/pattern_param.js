function foo({a,b}, [c,d]) {
}

function createCompiler({
  appName,
  config,
  devSocket,
  urls,
  useYarn,
  useTypeScript,
  webpack,
}) {
}

const x: Y = ({
   repoSlug,
   findings,
   filePath
}) => repoSlug;

foo.every(([a, {b, c}]) => a + b + c);

foo.every(([m, [n, {o, p}]]) => null);
