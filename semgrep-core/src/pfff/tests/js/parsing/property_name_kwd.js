DOM.create('div', {class: 'Hipal2_queryText Hipal2_errorQueryBox'}, '');

DOM.setAttributes(eDescript, {class: 'inputtext', value: descript});


function foo()
{
       try {
         if (!_iteratorNormalCompletion && _iterator.return != null) {
           _iterator.return();
         }
       } finally {
         if (_didIteratorError) {
           throw _iteratorError;
         }
      }
}
