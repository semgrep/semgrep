; Various definitions used throughout mlgrep, the OCaml reimplementation
; of the original Python semgrep.
;
; Eventually, this will be merged with the /utils library from semgrep-core
; or something similar.

(library
  (name mlgrep_util)
  (wrapped false)
  (libraries
    cmdliner
    commons        ; pfff
    semgrep_utils  ; semgrep-core
    semgrep_core
  )
)
