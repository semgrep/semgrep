rules:
  - id: classic
    pattern-sources:
      - source(...)
      - source1(...)
    pattern-sinks:
      - sink(...)
      - eval(...)
    pattern-sanitizers:
      - sanitize(...)
    message: A user input source() went into a dangerous sink()
    languages: [python, javascript]
    severity: WARNING
