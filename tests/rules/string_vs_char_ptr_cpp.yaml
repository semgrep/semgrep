rules:
  - patterns:
      - pattern: |
          $EXPR
      - metavariable-type:
          metavariable: $EXPR
          type: string
    id: string-type
    message: string-type
    languages:
      - cpp
    severity: WARNING

