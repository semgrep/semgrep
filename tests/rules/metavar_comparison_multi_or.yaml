rules:
  - id: multi-or
    severity: WARNING
    languages:
      - java
    message: test
    patterns:
      - pattern: return $X;
      - metavariable-comparison:
          comparison: $X < 2 or $X < 2 or $X < 2
          metavariable: $X
