rules:
  - id: test
    message: Test
    languages:
      - java
    severity: WARNING
    patterns:
      - pattern: private int $X;
      - metavariable-type:
          # $Y is not bound to anything and this should be reported by the
          # metachecker...
          metavariable: $Y
          type: int
