rules:
  - id: test-xml-matching
    languages:
      - xml
    message: Working!
    options:
      xml_children_ordered: false
    pattern: |
      <plugin>
        ...
        <artifactId>maven-compiler-plugin</artifactId>
        <groupId>org.apache.maven.plugins</groupId>
      </plugin>
    severity: WARNING
