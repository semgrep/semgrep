rules:
    - id: lower
      severity: WARNING
      languages:
          - python
      message: test
      patterns:
          - pattern: $VAR = $VALUE
          - metavariable-comparison:
                metavariable: $VALUE
                comparison: lower(str($VALUE)) == "semgrep"
