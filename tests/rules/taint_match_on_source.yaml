rules:
  - id: tainting
    message: Test
    severity: ERROR
    options:
      taint_focus_on: source
    mode: taint
    languages:
      - python
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
