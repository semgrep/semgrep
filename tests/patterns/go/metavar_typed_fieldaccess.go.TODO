package Foo

func main() {
    var s string
    var b bar
    var p point

    //Todo should be matched
    bar.x = make(s, p)
    //Todo should be matched
    bar.y = make(s)
    p.x = make(s)
    p.y = make(s)
}
