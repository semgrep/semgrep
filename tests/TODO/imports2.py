import os
import subprocess as sub
import requests
import sys

send = True

while send:
    sent = os.write(sys.stdout.fileno(), send)
    requests.get(url, foo=x, timeout=10, bar=z)
    send = send[sent:]
    p36 = sub.Popen(
        [sys.executable, "-c", script], encoding=encoding, stdout=subprocess.PIPE
    )
    p35 = s.Popen([sys.executable, "-c", script])
