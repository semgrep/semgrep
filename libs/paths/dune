; Library containing different way to process paths
(library
  (public_name paths)
  (name paths)
  (wrapped false)
  (libraries
     fpath

     commons
     profiling

     ; This is used in Git_project to find the project root. We could
     ; move this code around if the dependency is problematic.
     git_wrapper
  )
  (preprocess (pps
     ppx_deriving.show
     ppx_deriving.eq
     profiling.ppx
))
)
