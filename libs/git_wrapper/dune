; wrapper library around the 'git' command-line tool
(library
  (public_name git_wrapper)
  (name git_wrapper)
  (wrapped false)
  (flags (:standard -open No_TCB))
  (libraries
     fpath
     bos
     uuidm

     commons
  )
  (preprocess (pps
     ppx_deriving.show
))
)
