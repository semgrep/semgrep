class ObservableTextInputControl extends classWithMixins(TextInputControl, mixin(ArbiterMixin)) {
}

class ObservableTextInputControl extends classWithMixins(TextInputControl, mixin(ArbiterMixin)) {
}

class DPTokenizer extends classWithMixins(Tokenizer, mixin(ArbiterMixin)) {
}

class AlertManagerSearchQuery
     extends mixin(ArbiterMixin, FilterQueryMixin,
                  PageTransitionMixin, SearchTableColumnMixin) {
}

class TabularLayout extends mixin(new Layout()) {
}
