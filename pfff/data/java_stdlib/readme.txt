The files in subdirectories were generated via
 $ codegraph -lang java -build_stdlib /path/to/classpath/or/android/or/openjdk

I've removed them from the git repo, but look for the history
of this directory if you want to restore those files (or regenerate
them).
