those languages are currently skipped because we have a 1h limit for our CI
jobs in circleCI and they are not as important as the other languages,
so it's simpler to not slowdown the parsing-stats jobs with those languages.
