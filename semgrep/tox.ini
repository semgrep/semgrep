# content of: tox.ini , put in same dir as setup.py
[tox]
envlist = py36, py37, py38, py39

[testenv]
commands =
    pipenv install --sequential --dev
    pipenv run python -m pytest -n auto
setenv =
    # suppress a pipenv warning
    PIPENV_VERBOSITY = -1
