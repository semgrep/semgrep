rules:
  - id: eqeq-is-bad
    pattern-either:
      - pattern: $X == $X
      - tappern: whoops, typo
    message: "useless comparison operation `$X == $X` or `$X != $X`; possible bug?"
    severity: ERROR
