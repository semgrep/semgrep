rules:
  - id: ruby-sca
    pattern: bad()
    project-depends-on:
      - namespace: gem
        package: parallel
        version: "== 1.21.0"
    message: oh no
    languages: [ruby]
    severity: WARNING
