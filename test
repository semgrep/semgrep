#! /usr/bin/env bash
#
# This calls the test program generated by alcotest_ext.
# We're not using a symlink because dune rejects dangling symlinks.
# See './test --help' for usage and options.
#
./_build/default/src/tests/test.exe "$@"
